import { ComingSoon } from "@/components/coming-soon";

export default function StudioScreen() {
  return <ComingSoon title="Studio" note="A list of your bots' flows — name, status, and test results — is coming next." />;
}
