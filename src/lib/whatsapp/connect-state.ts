// Shared between the start and callback routes for the WhatsApp Embedded Signup redirect flow —
// a single source of truth for the cookie name so the two routes can't drift apart.
export const WHATSAPP_CONNECT_STATE_COOKIE = "wa_connect_state";
